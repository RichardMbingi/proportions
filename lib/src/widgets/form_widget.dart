//TODO: Create form template
import 'package:flutter/material.dart';

buildFormCard(context) {
  return showDialog(
    context: context,
    builder: (context) {
      AlertDialog(
        title: Text("Alert!!"),
      );
    },
  );
}
